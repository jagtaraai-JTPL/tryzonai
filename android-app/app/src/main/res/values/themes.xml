<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.TryZonAI" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@android:color/white</item>
    </style>

    <style name="CheckoutTheme" parent="Theme.Material3.Light.NoActionBar" />
</resources>
